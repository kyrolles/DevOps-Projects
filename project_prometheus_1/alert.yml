groups:
- name: disk-alerts 
  rules:
    - alert: when_disk_usage_is_above_80%
      expr: (1-(node_filesystem_avail_bytes / node_filesystem_size_bytes))*100 > 80
      for: 1m
      labels:
        severity: warning 
      annotations:
        summary: "disk usage is above 80%"
